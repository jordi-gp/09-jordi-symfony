{% extends 'base.html.twig' %}

{% block title %}Usuario{% endblock %}

{% block body %}
    <h1>Usuario</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ usuario.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ usuario.name }}</td>
            </tr>
            <tr>
                <th>Username</th>
                <td>{{ usuario.username }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ usuario.email }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ usuario.password }}</td>
            </tr>
            <tr>
                <th>Role</th>
                <td>{{ usuario.role }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ usuario.createdAt ? usuario.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Profile</th>
                <td>{{ usuario.profile }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_edit_usuario_index') }}">back to list</a>

    <a href="{{ path('app_edit_usuario_edit', {'id': usuario.id}) }}">edit</a>

    {{ include('edit_usuario/_delete_form.html.twig') }}
{% endblock %}
