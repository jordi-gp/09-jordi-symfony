{% extends 'default.html.twig' %}

{% block title %}Edit Usuario{% endblock %}

{% block main %}
<div class="d-flex flex-row justify-content-center mt-2 mb-4">
    <div class="col-3"></div>
    <div class="col-6 col-md-4">
        <h1>Edit Usuario</h1>
        {{ include('edit_usuario/_form.html.twig', {'button_label': 'Update'}) }}
        <div class="mt-3">
            {{ include('edit_usuario/_delete_form.html.twig') }}
        </div>
    </div>
    <div class="col-3"></div>
</div>
{% endblock %}
